name: txdx3_client
description: "TxDx3 - Open source, self-hosted personal productivity"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 3.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  bitsdojo_window: ^0.1.6
  cupertino_icons: ^1.0.2
  pocketbase: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
